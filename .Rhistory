data(mtdata)
data(mtcars)
table(mtcars$cyl)
stats <- function(x){
c(n = length(x),mean = mean(x), sd = sd())
}
stats(mtcars$mpg)
c(n = length(x),mean = mean(x), sd = sd(x))
stats <- function(x){
c(n = length(x),mean = mean(x), sd = sd(x))
}
stats(mtcars$mpg)
?by()
?split()
?sapply
split(mtcars, mtcars$cyl)
aggregate(mpg~cyl,data=mtcars,mean)
aggregate(mpg~cyl,data=mtcars,stats)
sapply(mtcars$cyl, stats )
length(cyl)
length(mpg~cyl)
aggregate(mpg~cyl,data=mtcars,mean)
aggregate(mpg~cyl,data=mtcars,stats)
?split()
length(mpg~cyl)
mean(mpg~cyl)
sd(mpg)
lapply(split(mtcars,mpg~cyl ))
lapply(split(mtcars,mpg~cyl,stats))
lapply(split(mtcars,stats))
lapply(split(mpg~cyl,stats))
split(mtcars, mpg~cyl
)
split(mtcars, mpg~cyl)
split(mtcars,cyl)
split(mpg$cyl,stats)
split(mtcars$cyl,stats)
split(mpg~cyl,stats)
g<-mpg$cyl
split(mpg,cyl)
split(mtcars$mpg,mtcars$cyl)
sapply(split(mtcars$mpg,mtcars$cyl),stats)
lappy(split(mtcars$mpg, mtcars$cyl),stats)
lapply(split(mtcars$mpg, mtcars$cyl),stats)
lapply(split(mtcars$mpg,mtcars$cyl),stats)
sapply(split(mtcars$mpg,mtcars$cyl),stats)
table <- sapply(split(df$x, df$y),stats)
stats <- function(df,x,y){
stats<-c(n = length(x),mean = mean(x), sd = sd(x))
table <- sapply(split(df$x, df$y),stats)
}
stats(mtcars, mpg, cyl)
mean(mtcars$mpg )
table(mtcars$cyl)
stats <- function(x,y){
stat<-c(n = length(x),mean = mean(x), sd = sd(x))
table <- sapply(split(x, y),stats)
}
stats(mtcars$mpg, mtcars$cyl)
stats <- function(x,y){
stat<-c(n = length(x),mean = mean(x), sd = sd(x))
table <- sapply(split(x, y),stat)
}
stats(mtcars$mpg, mtcars$cyl)
stat<-c(n = length(x),mean = mean(x), sd = sd(x))
oneway(mpg~cyl, mtcars)
oneway <- function(formula, data, ...){
#anova
fit<- lm(formula, data)
}
oneway(mpg~cyl, mtcars)
oneway <- function(formula, data, ...){
#anova
fit<- lm(formula, data)
return(fit)
}
oneway(mpg~cyl, mtcars)
x <- oneway(mpg~cyl, mtcars)
summary(x)
aggregate( mpg~ cyl, mtcars, function(x) c(n = lenth(x), mean = mean(x), sd = sd(x)))
aggregate( mpg~ cyl, mtcars, function(x) c(n = length(x), mean = mean(x), sd = sd(x)))
oneway <- function(formula, data, ...){
#anova
fit<- lm(formula, data)
#summary statistics
stats <- aggregate(formula, data,
function(x) c(n = length(x),
mean = mean(x),
sd = sd(x)))
# return results
result <- list(anova = fit, summarystats = stats)
return(result)
}
oneway(mpg~cyl, mtcars)
oneway <- function(formula, data, ...){
#delete missing data
data <- na.omit(data)
#anova
fit<- lm(formula, data)
#summary statistics
stats <- aggregate(formula, data,
function(x) c(n = length(x),
mean = mean(x),
sd = sd(x)))
# return results
result <- list(anova = fit, summarystats = stats)
return(result)
}
class(result)<- "oneway"
oneway <- function(formula, data, ...){
#delete missing data
data <- na.omit(data)
#anova
fit<- lm(formula, data)
#summary statistics
stats <- aggregate(formula, data,
function(x) c(n = length(x),
mean = mean(x),
sd = sd(x)))
# return results
result <- list(anova = fit, summarystats = stats)
class(result)<- "oneway"
return(result)
}
x <- oneway(mpg ~ cyl, mtcars)
class(x)
oneway
x
print.default(x)
print(x)
oneway <- function(formula, data, ...){
#delete missing data
data <- na.omit(data)
#anova
fit<- lm(formula, data)
#summary statistics
stats <- aggregate(formula, data,
function(x) c(n = length(x),
mean = mean(x),
sd = sd(x)))
# return results
result <- list(anova = fit, summarystats = stats)
class(result)<- "oneway"
return(result)
}
oneway(mpg~cyl,mtcars)
usethis::use_mit_license(name="Emma Tuhabonye")
library(oneway)
rm(oneoneway)
rm(oneway)
?oneway
library(oneway)
use_readme_me()
use_readme_md()
library(usethis)
use_readme_md()
library(pkgdown)
install.packages(pkgdown)
install.packages("pkgdown")
library(pkgdown)
buildsite()
build_site()
library(oneway)
?oneway
